{% extends "base.html" %}

{% block content %}
<h2 class="mb-4 text-center">All products</h2>

<div class="row row-cols-1 row-cols-sm2 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4 mb-5">

    {% for product in products %}

    <div class="col">
        <div class="card h-100 shadow-sm">
            <div>

            </div>
        </div>      

        <div class="card-body d-flex-column">
            <h5 class="card-title">{{ product.name }}</h5>
            <p class="card-text">{ {product.description} }</p>
            <div class="mt-auto fw-bold text-primary">{{ product.price }}</div>
        </div>

    </div>







      
    {% endfor %}

</div>    



{% endblock content %}